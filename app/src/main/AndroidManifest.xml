<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mobile.pk.com.yifymovies" >

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
        android:name="mobile.pk.com.yifymovies.Application">
        <activity
            android:name="mobile.pk.com.yifymovies.ui.activity.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <meta-data
                android:name="android.app.default_searchable"
                android:value=".ui.activity.MovieSearchActivity" />

        </activity>
        <activity android:name="mobile.pk.com.yifymovies.ui.activity.MovieDetailActivity"/>
        <activity android:name=".ui.activity.MovieSearchActivity">
            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />
            </intent-filter>
            <meta-data android:name="android.app.searchable"
                android:resource="@xml/searchable" />
        </activity>
        <activity android:name=".ui.fragments.MovieFilterActivity" android:label="@string/movie_filter"/>
    </application>

</manifest>